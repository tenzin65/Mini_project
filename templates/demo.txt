<div class="table-container"></div>
            <h2>Update your Todos</h2>
            
              
                {% if allTodo|length ==0 %}
                        
                        <div class="alert alert-dark" role="alert">
                           No Todos found. Add your first todo now!
                        </div>
                    {% else %}
                    <table class="table">
                        <thead>
                         <tr>
                            <th scope="col">S.NO</th>
                            <th scope="col">Title</th>
                            <th scope="col">Description</th>
                            <th scope="col">Date_Submit</th>
                            <th scope="col">Action</th>
                         </tr>
                        </thead>
                        <tbody>    
              {% for todo in allTodo %}
              <tr>
                <th scope="row">{{loop.index}}</th>
                <td>{{todo.title}}</td>
                <td>{{todo.desc}}</td>
                <td>{{todo.date_submit}}</td>
                <td>
                  <a href="/update/{{todo.sno}}"> <button class="custom-button" btn-sm mx-1>Update</button></a>
                  <a href="/delete/{{todo.sno}}"> <button class="custom-button" btn-sm mx-1>Delete</button></a>
                </td>
              </tr>
            </tbody>
        </table>
              {% endfor %}
              {% endif %}
          
           
           
    </div>